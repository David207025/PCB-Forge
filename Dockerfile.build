FROM rust:1.75-bookworm

# Install system dependencies required for WebKit/GTK bindings
RUN apt-get update && apt-get install -y \
    libgtk-3-dev \
    libwebkit2gtk-4.1-dev \
    pkg-config \
    build-essential \
    libssl-dev

WORKDIR /app